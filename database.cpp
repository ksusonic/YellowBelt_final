#include "database.h"

void Database::Add(const Date &date, const string &event) {

}

void Database::Print(ostream &os) const {

}

template<typename iter>
iter Database::FindIf(const bool &condition) const {
    return nullptr;
}

int Database::RemoveIf(const bool &criteria) {
    return 0;
}

string Database::Last(Date date) {
    return std::string();
}
